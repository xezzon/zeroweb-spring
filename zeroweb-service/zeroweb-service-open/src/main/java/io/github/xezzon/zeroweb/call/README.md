# 对外接口调用

## 目标
为第三方应用提供安全、可控的对外接口调用能力，支持接口转发、鉴权和调用日志。

## 主要功能
- 接口转发：根据订阅关系，将第三方请求转发到对应后端接口。
- 鉴权校验：校验AccessKey、签名、时间戳，签发JWT用于后端认证。
- 支持多种HTTP方法：GET、POST、PUT、DELETE、PATCH等。
- 调用权限校验：仅允许已订阅且审核通过的接口被调用。

## 约束
- 只有订阅并审核通过的接口才能被调用。
- 请求参数、请求头、请求体均按原样转发，敏感头部信息需移除。
